{
  "name": "simulateur-dimensionnement",
  "private": true,
  "version": "0.1.0",
  "description": "Simulateur de dimensionnement des secteurs techniciens — Techno Smart",
  "workspaces": [
    "packages/*",
    "web"
  ],
  "scripts": {
    "test": "npm run test --workspace @sim/engine",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "dev": "npm run dev --workspace web",
    "build": "npm run build --workspace web"
  },
  "engines": {
    "node": ">=20"
  }
}
